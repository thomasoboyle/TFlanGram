<div class="row">
  <div class="col">
    <h1><span>Workouts</span></h1>

    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Location</th>
          <th>Duration</th>
        </tr>
      </thead>

      <% @workouts.each do |workout| %>
        <tbody>
          <tr>
            <td><%= workout.date %></td>
            <td><%= workout.location %></td>
            <td><%= workout.duration %>Minutes</td>
            <td><%= link_to 'View', workout_path(workout) %></td>
            <td><%= link_to 'Edit', edit_workout_path(workout) %></td>
          </tr>
        </tbody>
      <% end %>

      </table>
    </br>

    <div class="col">
      <%= link_to '+', new_workout_path, class: 'btn-floating btn-large waves-effect waves-light red' %>
    </div>
  </div>
</div>
